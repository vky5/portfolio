import React from "react";
import { Cloud, Server, Briefcase, Code, Database, Globe } from "lucide-react";

export const IconMap: Record<
  string,
  React.ComponentType<{ className?: string }>
> = {
  Cloud,
  Server,
  Briefcase,
  Code,
  Database,
  Globe,
};
